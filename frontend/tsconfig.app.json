{
  "compilerOptions": {
    /* ✅ Language & Environment */
    "target": "ESNext", // Use latest JS features
    "lib": ["ESNext", "DOM", "DOM.Iterable"], // DOM for browser support, ESNext for latest

    /* ✅ Module Resolution */
    "module": "ESNext",
    "moduleResolution": "bundler", // Vite-specific, Chakra v3 expects this
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "noEmit": true, // Don't emit JS files, Vite handles that
    "moduleDetection": "force",

    /* ✅ JSX & React */
    "jsx": "react-jsx", // For React 17+ JSX runtime

    /* ✅ Paths (Optional: cleaner imports) */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    /* ✅ Linting & Type Safety */
    "strict": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noUncheckedSideEffectImports": true,

    /* ✅ Caching (Optional: faster builds) */
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo"
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
